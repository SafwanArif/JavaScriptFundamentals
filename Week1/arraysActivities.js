// Activity 1: Make an array for 3 of your favourite songs and log them to the console.

let favouriteSongs = [
    "The Game - Ol' English",
    "Eminem ft. Natt Dogg - 'Till I Collapse",
    "Akon - Locked Up"
]

console.log(favouriteSongs)

// STRETCH - Add another 2 songs to the list using a method and then remove the last one added.

favouriteSongs.push("Nas ft. Ms. Lauryn Hill - If I Ruled the World (Imagine That)", "Dr. Dre ft. Snoop Dogg - The Next Episode") // adds to end of array.

console.log(favouriteSongs)

favouriteSongs.pop() // removes the last element from an array.

console.log(favouriteSongs)

// Activity 2: Using MDN website choose one of the follwing methods: map(), shift(), unshift(), splice(), unsplice()
// Create a program to demonstrate the use of the method.


// favouriteSongs.unshift("T-Pain - I'm Sprung") // Inserts a new element at the start of the array.

// console.log(favouriteSongs)

// favouriteSongs.shift() // Removes the first elements at the start of array.

// console.log(favouriteSongs)

// const months = ["January", "March", "April", "July"]

// console.log(months)

// months.splice(1, 0, "Febuary") // Adds an element in the array in specific place.

// console.log(months)

// months.splice(4, 1, "May") // Adds an element in the array in specific place and then removes the 

// console.log(months)

// let array1 = [1, 4, 9, 16]

// let map1 = array1.map(x => x * 2)

// console.log(map1)

